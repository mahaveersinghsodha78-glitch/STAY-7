# Stay7 Hotel Booking (Frontend)

This is the frontend for **Stay7 Hotel Booking App** built with React + Vite + TailwindCSS.

## Run locally
```bash
npm install
npm run dev
```

## Build for production
```bash
npm run build
```

Deploy on **Vercel** with:
- Build Command: `npm run build`
- Output Directory: `dist`
